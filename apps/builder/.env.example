# Builder app (Next.js) — talks to Postgres directly via tRPC + @orgsites/db,
# no separate API base URL needed for the editor itself.
DATABASE_URL="postgresql://orgsites:orgsites@localhost:5432/orgsites"
