/**
 * Placeholder for shared config (ESLint rules, shared Tailwind theme
 * tokens matching the brand colors in packages/db's Site.themeColors
 * default, etc.) once the builder/renderer apps' styling has settled.
 */
export {};
