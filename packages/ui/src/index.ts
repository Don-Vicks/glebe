/**
 * Placeholder. Intended home for block-rendering components shared between
 * apps/builder/src/editor/puck-config.tsx and
 * apps/renderer/src/blocks/registry.tsx, which currently duplicate their
 * markup. Extracting each block's visual output into a component here
 * (e.g. `HeroBlock`, `MissionBlock`) would let Puck's live-preview and the
 * published site render pixel-identical output from one source — worth
 * doing once you've settled on the remaining 6 block types' final designs
 * rather than before.
 */
export {};
